name: Launch VS Code Dev Server

on:
  # 手動でワークフローを実行できるようにする
  workflow_dispatch:

jobs:
  dev-server:
    # Ubuntuの最新版ランナーを使用
    runs-on: ubuntu-latest
    # セキュリティのため、必要な権限のみに絞る
    permissions:
      contents: read

    # ジョブのタイムアウトを60分に設定（任意）
    timeout-minutes: 60

    steps:
      # 1. リポジトリのコードをチェックアウト
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. セッション用のランダムなパスワードを生成
      - name: Generate Password
        run: |
          # opensslを使って32文字のランダムな文字列を生成
          PASSWORD=$(openssl rand -base64 32)
          # 生成したパスワードをログに一度だけ表示
          echo "🔐 The password for this session is: ${PASSWORD}"
          # これ以降のログでパスワードがマスクされるように設定
          echo "::add-mask::${PASSWORD}"
          # 後続のステップで使えるように環境変数に保存
          echo "PASSWORD=${PASSWORD}" >> $GITHUB_ENV

      # 3. code-serverとngrokを起動
      - name: Start code-server and ngrok
        run: |
          # ngrokをダウンロード
          curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null && echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list && sudo apt update && sudo apt install ngrok
          
          # Dockerでcode-serverをバックグラウンドで起動
          # - ポート8080で起動
          # - 生成したパスワードを設定
          # - リポジトリのルートディレクトリをコンテナの/home/coder/projectにマウント
          docker run -d --name code-server -p 127.0.0.1:8080:8080 \
            -e "PASSWORD=${PASSWORD}" \
            -v "${{ github.workspace }}:/home/coder/project" \
            codercom/code-server:latest

          # ngrokをバックグラウンドで起動し、ローカルの8080番ポートにトンネル
          ngrok http 8080 > /dev/null &

          # ngrokが起動するまで少し待つ
          sleep 5

          # ngrokのAPIから公開URLを取得
          URL=$(curl -s http://127.0.0.1:4040/api/tunnels | jq -r '.tunnels[0].public_url')

          # 取得したURLをログに出力
          echo "🚀 Access your VS Code session at: ${URL}"
          # 後続のステップで使えるように環境変数に保存
          echo "URL=${URL}" >> $GITHUB_ENV

      # 4. ワークフローが終了しないように待機
      - name: Keep the server alive
        run: |
          echo "✅ VS Code server is running."
          echo "🛑 To stop the server, cancel this workflow run."
          # ワークフローが手動でキャンセルされるまで実行し続ける
          while true; do
            sleep 30
            # ハートビートとして定期的にログを出力
            echo "🔄 Server is still running at $(date)"
          done
